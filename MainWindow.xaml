<Window x:Class="лаб27.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2000/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2000/xaml"
        Title="Лабораторная работа 27-1. Вариант 22" Height="300" Width="450"
        Background="#FFF0F5">
    <Window.Resources>
        <Style TargetType="Control">
            <Setter Property="FontFamily" Value="Lucida Sans, Lucida Bright" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontStyle" Value="Italic" />
            <Setter Property="Foreground" Value="Black" />
        </Style>

        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Control}}">
            <Setter Property="Background" Value="#FF69B4" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row0="0" Text="Введите строку с латинскими буквами и скобками:" 
                   Margin="0,0,0,10" Style="{StaticResource {x:Type Control}}"/>

        <TextBox x:Name="InputTextBox" Grid.Row="1" Height="30" VerticalContentAlignment="Center" Margin="0,0,0,15"/>

        <Button Grid.Row="2" Content="Проверить скобки" Height="35" Click="CheckBrackets_Click"/>

        <TextBlock x:Name="ResultTextBlock" Grid.Row="3" Margin="0,15,0,0" 
                   HorizontalAlignment="Center" FontSize="14" FontWeight="Bold"/>
    </Grid>
</Window>